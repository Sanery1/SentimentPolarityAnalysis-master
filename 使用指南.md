# 📘 情感分析系统 - 使用指南

> 完整的安装、部署和使用说明

---

## 📦 目录

- [快速开始](#快速开始)
- [环境配置](#环境配置)
- [使用方法](#使用方法)
- [API 调用](#api-调用)
- [项目结构](#项目结构)
- [常见问题](#常见问题)
- [分享说明](#分享说明)

---

## 🚀 快速开始

### 第一步：安装 Python

确保已安装 **Python 3.7 或更高版本**：
- 下载地址：https://www.python.org/downloads/
- **重要**：安装时勾选 "Add Python to PATH"

验证安装：
```bash
python --version
```

### 第二步：安装依赖

在项目根目录下运行：
```bash
pip install -r requirements.txt
```

> 💡 **提示**：如果下载慢，可使用国内镜像：
> ```bash
> pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
> ```

### 第三步：启动项目

**方式一：Web 界面（推荐）⭐**
- **Windows**：双击 `启动Web服务.bat`
- **Mac/Linux**：运行 `python app.py`
- 在浏览器打开：http://localhost:5000

**方式二：命令行**
```bash
python run_demo.py
```

### 第四步：开始使用

**Web 界面：**
1. 在文本框输入句子（如："味道很好，服务也不错"）
2. 点击「🔍 分析情感」按钮
3. 查看结果（正面 😊 / 负面 😞）
4. 支持批量添加和分析多个句子

**命令行：**
- 运行 `python run_demo.py` 查看预设示例的分析结果

---

## 🔧 环境配置

### 系统要求

- **操作系统**：Windows 10+ / macOS 10.14+ / Linux
- **Python**：3.7 - 3.10
- **内存**：至少 2GB RAM
- **存储**：至少 500MB 可用空间

### 依赖包说明

项目依赖以下 Python 包（已包含在 `requirements.txt`）：

| 包名 | 版本 | 用途 |
|------|------|------|
| numpy | ≥1.19.0 | 数值计算 |
| scikit-learn | ≥0.24.0 | 机器学习算法 |
| jieba | ≥0.42.0 | 中文分词 |
| xlwt | ≥1.3.0 | Excel 输出 |
| flask | ≥2.0.0 | Web 框架 |
| flask-cors | ≥3.0.0 | 跨域支持 |

---

## � 使用方法

### 1. Web 界面使用

#### 启动服务器

**Windows 用户：**
```bash
# Windows 用户方式1：双击启动脚本
启动Web服务.bat

# Windows 用户方式2：命令行
python app.py
```

**Mac/Linux 用户：**
```bash
python app.py
```

启动成功后，你会看到：
```
============================================================
情感分析 Web 应用启动中...
============================================================
正在初始化情感分类器...
分类器初始化完成！

服务器启动成功！
请在浏览器中打开: http://localhost:5000
按 Ctrl+C 停止服务器
============================================================
```

#### 1.2 使用 Web 界面

打开浏览器访问 `http://localhost:5000`，你将看到：

**单句分析：**
1. 在输入框中输入一句话（如"味道很好，服务也不错"）
2. 点击"分析情感"按钮
3. 查看分析结果（正面/负面）

**批量分析：**
1. 点击"批量分析"标签
2. 每行输入一句话，可以输入多句
3. 点击"批量分析"按钮
4. 查看所有句子的分析结果列表

#### 1.3 API 调用

**Python 示例：**
```python
import requests

# 单句分析
response = requests.post('http://localhost:5000/api/analyze', 
                        json={'text': '味道很好，服务也不错'})
print(response.json())
# 输出：{'text': '味道很好，服务也不错', 'sentiment': 'positive', 'score': 1}

# 批量分析
response = requests.post('http://localhost:5000/api/batch_analyze',
                        json={'texts': ['很好吃', '太难吃了', '一般般']})
print(response.json())
```

**JavaScript 示例：**
```javascript
// 单句分析
fetch('http://localhost:5000/api/analyze', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({text: '味道很好，服务也不错'})
})
.then(res => res.json())
.then(data => console.log(data));

// 批量分析
fetch('http://localhost:5000/api/batch_analyze', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({texts: ['很好吃', '太难吃了', '一般般']})
})
.then(res => res.json())
.then(data => console.log(data));
```

---

## 2. 命令行使用

### 2.1 使用词典分类器（无需训练）

```python
from spa.classifiers import DictClassifier

# 初始化
classifier = DictClassifier()

# 分析句子
result = classifier.analyse_sentence("味道很好，服务也不错")
print("正面" if result == 1 else "负面")  # 输出：正面
```

#### 2.2 使用机器学习分类器

```python
from spa.corpus import WaimaiCorpus
from spa.feature_extraction import ChiSquare
from spa.classifiers import BayesClassifier

# 1. 加载语料
corpus = WaimaiCorpus()
train_data, train_labels = corpus.get_train_corpus(1000)
test_data, test_labels = corpus.get_test_corpus(200)

# 2. 特征选择
chi_square = ChiSquare(train_data, train_labels)
best_words = chi_square.best_words(2000)

# 3. 训练
classifier = BayesClassifier(train_data, train_labels, best_words)

# 4. 预测
for data in test_data[:5]:
    result = classifier.classify(data)
    print("正面" if result == 1 else "负面")
```

### 2.3 运行完整实验

编辑并运行 `spa/test.py` 中的实验函数：

```python
# 在 Python 交互式环境或脚本中
from spa.test import test_dict

# 运行词典分类器实验
test_dict()
```

或者取消 `test.py` 底部的注释来运行其他实验：
```python
# test_waimai()   # 外卖数据集
# test_hotel()    # 酒店数据集
# test_movie2()   # 电影数据集
```

---

## 3. 数据集与分类器

### 3.1 可用的数据集

| 数据集 | 类名 | 语言 | 领域 | 规模 |
|--------|------|------|------|------|
| 外卖评论 | `WaimaiCorpus` | 中文 | 外卖 | 4000正+4000负 |
| 外卖评论2 | `Waimai2Corpus` | 中文 | 外卖 | - |
| 酒店评论 | `HotelCorpus` | 中文 | 酒店 | - |
| 电影评论 | `MovieCorpus` | 英文 | 电影 | - |
| 电影评论2 | `Movie2Corpus` | 英文 | 电影 | - |

### 3.2 支持的分类器

| 分类器 | 类名 | 特点 | 需要训练 |
|--------|------|------|---------|
| 词典规则 | `DictClassifier` | 基于情感词典和规则，支持中文 | ❌ |
| 朴素贝叶斯 | `BayesClassifier` | 快速、简单、效果好 | ✅ |
| K近邻 | `KNNClassifier` | 可设置 K 值 | ✅ |
| 最大熵 | `MaxEntClassifier` | GIS 算法（较慢） | ✅ |
| 支持向量机 | `SVMClassifier` | 基于 sklearn，效果通常最好 | ✅ |

### 3.3 超参数调优建议

**朴素贝叶斯：**
- `feature_num`: 2000-5000（特征词数量）

**KNN：**
- `k`: 3, 5, 7, 9, 11, 13（奇数避免平局）
- `feature_num`: 3000-5000

**SVM：**
- `C`: 10, 50, 100, 150（正则化参数）
- `feature_num`: 3000-5000

**最大熵：**
- `max_iter`: 100-500（迭代次数）
- `feature_num`: 2000-4000

---

## 4. 示例代码片段

### 4.1 测试不同的句子

```python
from spa.classifiers import DictClassifier

classifier = DictClassifier()

sentences = [
    "味道很好，服务也不错",
    "太难吃了，再也不来了",
    "要是米饭再多点儿就好了",
    "环境一般，价格有点贵",
    "非常满意，下次还会再来"
]

for sent in sentences:
    result = classifier.analyse_sentence(sent)
    print(f"{sent} => {'正面' if result == 1 else '负面'}")
```

### 4.2 比较多个分类器

```python
from spa.corpus import WaimaiCorpus
from spa.feature_extraction import ChiSquare
from spa.classifiers import BayesClassifier, KNNClassifier, SVMClassifier

# 准备数据
corpus = WaimaiCorpus()
train_data, train_labels = corpus.get_train_corpus(500)
test_data, test_labels = corpus.get_test_corpus(100)

# 特征选择
chi = ChiSquare(train_data, train_labels)
best_words = chi.best_words(1000)

# 训练多个分类器
classifiers = {
    "贝叶斯": BayesClassifier(train_data, train_labels, best_words),
    "KNN": KNNClassifier(train_data, train_labels, k=5, best_words=best_words),
    "SVM": SVMClassifier(train_data, train_labels, best_words, C=10)
}

# 评估
for name, clf in classifiers.items():
    results = [clf.classify(data) for data in test_data]
    acc = sum([1 for i in range(len(test_labels)) 
               if test_labels[i] == results[i]]) / len(test_labels)
    print(f"{name} 准确率: {acc*100:.2f}%")
```

### 4.3 保存和加载结果

```python
from spa.tools import Write2File, get_accuracy

# 评估并保存
origin_labels = test_labels
classify_labels = [classifier.classify(data) for data in test_data]

results = get_accuracy(origin_labels, classify_labels, 
                      [len(train_data), len(test_data), len(best_words)])

Write2File.write_contents("spa/f_runout/my_results.xls", results)
print("结果已保存到 spa/f_runout/my_results.xls")
```

---

## 5. 项目结构

```
SentimentPolarityAnalysis-master/
│
├── app.py                      # Flask Web 服务器
├── 启动Web服务.bat              # Windows 快速启动脚本
├── requirements.txt            # Python 依赖列表
├── README.md                   # 项目主文档
├── 使用指南.md                  # 本文档
│
├── spa/                        # 核心代码包
│   ├── __init__.py             # 包初始化文件
│   ├── classifiers.py          # 5种分类器实现
│   ├── corpus.py               # 数据集加载
│   ├── feature_extraction.py  # 卡方特征选择
│   ├── tools.py                # 工具函数
│   ├── test.py                 # 实验脚本
│   │
│   ├── f_corpus/               # 语料库文件
│   │   ├── ch_hotel_corpus.txt
│   │   ├── ch_waimai_corpus.txt
│   │   ├── ch_waimai2_corpus.txt
│   │   ├── en_movie_corpus.txt
│   │   └── en_movie2_corpus.txt
│   │
│   ├── f_dict/                 # 情感词典
│   │   ├── positive_dict.txt   # 正面词典
│   │   ├── negative_dict.txt   # 负面词典
│   │   ├── adverb_dict.txt     # 副词词典
│   │   ├── denial_dict.txt     # 否定词典
│   │   ├── conjunction_dict.txt # 连词词典
│   │   ├── punctuation_dict.txt # 标点权重
│   │   ├── phrase_dict.txt     # 短语词典
│   │   └── user.dict           # jieba 自定义词典
│   │
│   └── f_runout/               # 实验结果输出目录
│       └── *.xls               # Excel 格式结果文件
│
├── templates/                  # Web 前端模板
│   └── index.html              # 响应式 Web 界面
│
└── files/                      # 项目相关文件
    └── pic/                    # 图片资源
```

---

## 6. 常见问题

### Q1: Web 服务无法启动？

**可能原因：**
- 端口 5000 被占用
- Python 依赖未安装

**解决方法：**
```bash
# 检查端口占用（Windows）
netstat -ano | findstr :5000

# 安装依赖
pip install -r requirements.txt
```

### Q2: 如何查看详细的分类过程？

使用 `DictClassifier` 的 `print_show` 参数：
```python
classifier.analyse_sentence("很好吃", print_show=True)
```

### Q3: 如何保存实验结果？

实验结果会自动保存到 `spa/f_runout/` 目录，文件名包含时间戳。

或手动保存：
```python
from spa.tools import Write2File
Write2File.write_contents("spa/f_runout/my_results.xls", results)
```

### Q4: 如何添加自己的词典？

编辑 `spa/f_dict/` 下的相应文件，格式为：
```
词语 权重
```

例如在 `positive_dict.txt` 中添加：
```
美味 3
好吃 2
不错 1
```

### Q5: 如何使用自己的数据集？

参考 `spa/corpus.py` 中的 `Corpus` 类，创建你自己的语料类：

```python
class MyCorpus(Corpus):
    def __init__(self):
        corpus_path = os.path.join(_module_dir, "f_corpus/my_corpus.txt")
        super(MyCorpus, self).__init__(corpus_path)
```

### Q6: 浏览器无法访问 Web 界面？

确保：
1. 服务器已成功启动（查看终端输出）
2. 访问正确地址：`http://localhost:5000`
3. 防火墙未阻止端口 5000
4. 浏览器未禁用 JavaScript

---

## 7. 分享给他人

### 7.1 必需文件清单

分享时确保包含以下文件和目录：

✅ **核心文件：**
- `app.py` - Web 服务器
- `启动Web服务.bat` - Windows 启动脚本（可选）
- `requirements.txt` - 依赖列表
- `README.md` - 主文档
- `使用指南.md` - 本文档

✅ **核心代码包：**
- `spa/` 整个目录，包括：
  - 所有 `.py` 文件
  - `f_corpus/` - 语料库
  - `f_dict/` - 情感词典
  - `f_runout/` - 输出目录（可为空）

✅ **Web 前端：**
- `templates/index.html`

### 7.2 接收者安装步骤

1. **安装 Python**（版本 3.7+）
2. **解压项目文件**到任意目录
3. **安装依赖**：
   ```bash
   pip install -r requirements.txt
   ```
4. **启动服务**：
   - Windows: 双击 `启动Web服务.bat`
   - 或命令行：`python app.py`
5. **打开浏览器**访问 `http://localhost:5000`

### 7.3 检查清单

**分享前确认：**
- [ ] 已测试项目可以正常运行
- [ ] 包含所有必要文件
- [ ] `requirements.txt` 完整
- [ ] 文档清晰易懂

**接收后确认：**
- [ ] Python 已安装（`python --version`）
- [ ] 依赖已安装完成
- [ ] 可以成功启动服务
- [ ] 界面可以正常访问

---

## 8. 项目亮点

✨ **多种算法**：支持 5 种不同的分类方法（词典、贝叶斯、KNN、最大熵、SVM）  
✨ **中英文支持**：内置多个中英文数据集  
✨ **开箱即用**：词典分类器无需训练即可使用  
✨ **Web 界面**：现代化响应式设计，支持 PC 和移动端  
✨ **RESTful API**：便于集成到其他应用  
✨ **完整流程**：从数据加载、特征提取到模型评估的完整实现  
✨ **可扩展**：易于添加新的词典、数据集和分类器

---

## 📚 更多信息

- **完整技术文档**：查看 `README.md`
- **源代码说明**：查看 `spa/` 目录下的各个 Python 文件
- **实验示例**：查看 `spa/test.py` 中的示例代码

---

**祝使用愉快！** 🎉  
如有问题，请查阅 `README.md` 或检查 `spa/test.py` 中的示例代码。
